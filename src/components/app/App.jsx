import React from "react";
import * as styles from './app.module.scss';

const App = () => {
    return (
        <div className={ styles.title }>
            Hello, React!
        </div>
    )
};

export default App;